<p id="notice"><%= notice %></p>

<h1>Listing Projects</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.name %></td>
        <td><%= link_to 'Show', project %></td>
        <td><%= link_to 'Edit', edit_project_path(project) %></td>
        <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>

<%= link_to 'New Project', new_project_path %>
</p>
</br>
<fieldset>
<p><%= link_to 'List of Users', users_path %></p>
<p><%= link_to 'PM Knowledge areas',  knowledge_areas_path %></p>
<p><%= link_to 'List of PMBoK Documents', pmbok_docs_path %></p>
<p><%= link_to 'List of PM Process Groups', process_groups_path %></p>
  <p><%= link_to 'List of PM Sub Process Groups', subprocess_groups_path %></p>
</fieldset>
