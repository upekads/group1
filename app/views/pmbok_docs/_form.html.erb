<%= form_for(@pmbok_doc) do |f| %>
  <% if @pmbok_doc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pmbok_doc.errors.count, "error") %> prohibited this pmbok_doc from being saved:</h2>

      <ul>
      <% @pmbok_doc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
